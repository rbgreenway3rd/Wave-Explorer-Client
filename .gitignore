# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/.vscode
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# WSL "mark of the web" metadata files
*:Zone.Identifier

# Oversized real-data captures (271MB — exceeds GitHub's 100MB limit AND
# must stay out of public/ so it's never bundled into the build/deploy).
# Kept here for the realData.harness diagnostic test, which self-skips when
# absent. Host externally / use Git LFS if it must be versioned.
/local-fixtures/
